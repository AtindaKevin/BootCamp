"""Write a function canner_can that takes a single argument item in the form of a 
string, and returns the string 'Can you can a [STRING] as a canner can can a can?', 
where '[STRING]' takes the value of item"""

#def canner_can():
    #print()


"""Define function that calculates the number of seconds in a year."""
#def number_of_seconds(days=365):
 #   return days = 

"""define a function which finds the maximum number among three numbers:
def max_number(x,y,z):
    my_list = [x,y,z]
    my_list.sort()
    highest_number = my_list[-1]
    return highest_number

print(max_number(23,43,17))


def maximum(a, b, c): 
  
    if (a >= b) and (a >= c): 
        largest = a 
  
    elif (b >= a) and (b >= c): 
        largest = b 
    else: 
        largest = c 
          
    return largest 

print(maximum(23,45,62))"""
"""Write a function between_len that takes three arguments:
    a list words;
    an integer minlen; and
    an integer maxlen.
The function should return True if the length of words is at least minlen 
and no more than maxlen, and False otherwise."""

def between_len(x,y,x):
    

    return words
print(between_len("a list words", "an integer minlen", "an integer maxlen"  ))
